# minimal config for powermon
device:
  name: Test_Inverter
  id: 123456789
  model: 8048MAX
  manufacturer: MPP-Solar
  port:
    type: test
    response_number: 0
    protocol: PI30

commands:
# - command: QMCHGCR
#   outputs:
#   - type: screen
#     format: raw
- command: QPIGS
  outputs:
  - type: screen
    format:
      type: topics
      draw_lines: True
  # type: continuous
  # trigger: # default to something sensible (only one of the below is needed/used)
  # - loops: 1 # run every loop
